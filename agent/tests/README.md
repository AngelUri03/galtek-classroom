# Agent Tests

This directory contains automated tests for the C# Agent solution.

Implemented test coverage starts with Installation Identity, Machine Code, Commercial License and Local IPC behavior in `GaltekClassroom.Agent.Service.Tests`.

`GaltekClassroom.Agent.Session.Tests` covers Session Agent command modes, per-session single instance locking and IPC supervisor lifecycle behavior.

package com.galtek.classroom.performance;

public enum IdleClientActivity {
    CONTROL_CONNECTION,
    HEARTBEAT,
    CAPTURE,
    OVERLAY,
    UI,
    PROCESS_SCANNING,
    FILESYSTEM_SCANNING,
    WMI_QUERY,
    INVENTORY,
    PREFETCH,
    PERIODIC_DISK_WRITE,
    HEALTHY_HEARTBEAT_LOG,
    DIAGNOSTIC_SAMPLE
}
